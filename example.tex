\documentclass[a4paper,11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{enumerate}
\usepackage{listings}

% Use \newif\if{{<number>}} to define boolean flags for each cumulative level,
% and explicitly set them all to false using \{{<number>}}false
\newif\if{{2}} % 2nd level, includes base contents
\newif\if{{3}} % 3rd level, includes 2nd level contents and base contents...
\{{2}}false
\{{3}}false

\title{Example}
\begin{document}
	\maketitle
	\begin{enumerate}
		\item This item is part of the base contents of the document.\if{{2}}{ This sentence is only shown at levels 2 and above.}\fi
		\if{{2}}
			\item This item is only shown at levels 2 and above.\if{{3}}{ This sentence is only shown at level 3.}\fi
		\fi
	\end{enumerate}
\end{document}
